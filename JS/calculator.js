export function calculatePerformance(data) {
    // Aquí iría la lógica de cálculo usando los datos del formulario
    // Por ejemplo, devolver un objeto con los resultados
    return {
        processor1: { /* Resultados del procesador 1 */ },
        processor2: { /* Resultados del procesador 2 */ }
    };
}
